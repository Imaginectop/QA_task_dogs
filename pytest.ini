[pytest]
addopts = -q --disable-warnings --maxfail=1
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Маркеры (пример)
markers =
    network: тесты, делающие реальные HTTP-запросы
    slow: долгие тесты
    smoke: базовые быстрые проверки
